apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-logistik-frontend.fullname" . }}
  labels:
    {{- include "helm-logistik-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-logistik-frontend.selectorLabels" . | nindent 4 }}
